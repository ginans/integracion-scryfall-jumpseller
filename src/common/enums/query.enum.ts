export enum SortOrder {
    ASC = 'asc',
    DESC = 'desc'
}

export enum StateCards {
    PENDING = 'pending',
    COMPLETED = 'completed',
    FAILED = 'failed',
}

export enum EnumLang {
    ES = 'es',
    EN = 'en',
}